# Changelog

## [0.2.0] (pairs with StatusWindows 0.2.0)

- Version bump only, to bring the bundle's number into lockstep with
  StatusWindows. No change to what the package does.

## [0.1.0] (pairs with StatusWindows 0.2.0)

- Initial release: re-exports StatusWindows and MathTeXEngine so that
  `using StatusWindowsTeX` enables `math!`.
